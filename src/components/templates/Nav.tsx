export default function Nav() {
    return (
        <nav className="relative bg-gray-900 p-8 flex flex-col justify-center items-center text-violet-50">
            Navigation 영역
        </nav>
    )
}